# 벼리 백엔드 시크릿 — 실제 값은 infra/.env 에 작성(커밋 금지)
# docker compose의 byeori-api 서비스가 env_file로 읽어 컨테이너에 주입한다.

# TourAPI (data.go.kr 15101578) — "일반 인증키(Decoding)" 값을 넣을 것
TOURAPI_KEY=

# KOPIS (kopis.or.kr) 서비스키
KOPIS_KEY=
